# Drag and Drop

Demonstrates dragging elements between positions on a kanban board.

## Features demonstrated

- `drag` action with `text` targeting and `within` scoping
- `moveTo` action to preview hover before dragging
- Animated cursor movement during drag operations

## Run

```bash
cd examples/drag-and-drop
webreel record
```
