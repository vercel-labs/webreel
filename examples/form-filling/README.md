# Form Filling

Demonstrates typing into form fields and clicking a submit button, simulating a login flow.

## Features demonstrated

- `type` action with `target` (selector-based targeting)
- `type` action with custom `delay` for realistic typing speed
- `click` action using `selector` targeting

## Run

```bash
cd examples/form-filling
webreel record
```
