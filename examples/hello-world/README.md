# Hello World

The simplest possible webreel example. Opens a landing page and clicks the call-to-action button.

## Features demonstrated

- Minimal `webreel.config.json` setup
- `pause` action
- `click` action using `selector` targeting
- `waitFor` to ensure the page is ready before recording

## Run

```bash
cd examples/hello-world
webreel record
```
