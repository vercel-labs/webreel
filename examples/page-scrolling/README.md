# Page Scrolling

Demonstrates scrolling the page and scrolling within a specific container element on a blog post layout.

## Features demonstrated

- `scroll` action for full-page vertical scrolling
- `scroll` action with negative `y` to scroll back up
- `scroll` action with `selector` to scroll within a sidebar element

## Run

```bash
cd examples/page-scrolling
webreel record
```
