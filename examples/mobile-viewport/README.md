# Mobile Viewport

Demonstrates recording at mobile device dimensions using a finance app interface.

## Features demonstrated

- `viewport` set to mobile dimensions (390x844, iPhone 15 Pro)
- `scale` set to `2` for Retina-density rendering
- Mobile-specific interactions (hamburger menu tap, scroll)

## Run

```bash
cd examples/mobile-viewport
webreel record
```
