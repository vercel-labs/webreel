import { ExamplesGallery } from "../../components/examples-gallery";

export const metadata = {
  title: "Examples",
  description:
    "Browse example webreel configs: hello world, form filling, drag and drop, keyboard shortcuts, and more.",
};

# Examples

Self-contained examples that each include their own HTML page, config file, and README. Clone the repo and run any example:

```bash
cd examples/hello-world
webreel record
```

<ExamplesGallery />
